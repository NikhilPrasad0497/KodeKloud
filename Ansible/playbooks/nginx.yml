---
- hosts: web1
  become: true
  tasks:
    - name: install nginx
      yum:
        name: nginx
        state: present
    
    - name: Start and Enable nginx
      service:
        name: nginx
        state: started
        enabled: yes
